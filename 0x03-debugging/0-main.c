#include "main.h"

/**
 * main - Entry point
 * test function for positive or negative number
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return(0);
}
