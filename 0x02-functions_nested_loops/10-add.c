#include "main.h"
#include <stdio.h>

/**
 * add - Entry point
 * a function that adds two integers and returns the result
 * @k: sum of i and j
 * @i: integer datatype
 * @j: integer datatype
 *
 * Return: Sum of values
 */

int add(int i, int j)
{
	int k;

	k = i + j;
	return (k);
}
